# dependencies
node_modules/

# next.js
.next/
out/
build/
*.tsbuildinfo
next-env.d.ts

# testing
coverage/
playwright-report/
test-results/

# generated evidence — curated copies live in docs/reports/<slug>/img instead
artifacts/*
!artifacts/.gitkeep

# generated report PDFs (share them, don't version them)
docs/reports/*.pdf

# env — only the example is committed
.env
.env.*
!.env.example

# misc
.DS_Store
*.pem
.vercel
.idea/
.vscode/*
!.vscode/settings.json
!.vscode/extensions.json

# local agent scratch
.claude/settings.local.json
CLAUDE.local.md

# stray temp file
_spd.tmp
